content:
  application/json:
    schema:
      properties:
        message:
          description: Повідомлення про те, що запитуваний ресурс не існує.
          example: Контакт не знайдено
          type: string
        status:
          description: HTTP статус код 404, що означає, що ресурс не знайдено.
          example: 404
          type: integer
      required:
      - status
      - message
      type: object
description: Помилка "Не знайдено"
